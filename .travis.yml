language: java

sudo: false

dist: trusty

cache:
  directories:
  - "$HOME/.gradle"

os:
  - linux

jdk:
  - openjdk12

addons:
  sonarcloud:
    organization: "codetripper-github"

script:
  - ./gradlew sonarqube